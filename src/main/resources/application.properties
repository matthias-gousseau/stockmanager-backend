spring.application.name=stockmanager-backend

# Configuration du serveur
server.port=8080

# Configuration de la base de données PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/stockmanager
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=VotreCleSecreteSuperlonguePourJWT123456789VotreCleSecreteSuperlongue
jwt.expiration=86400000

# CORS (pour Angular sur localhost:4200)
allowed.origins=http://localhost:4200

# Logs
logging.level.org.springframework.security=DEBUG
logging.level.com.stockmanager=DEBUG