spring.application.name=stockmanager-backend

# Configuration du serveur
server.port=8080

# Configuration de la base de données PostgreSQL POUR LES TESTS
spring.datasource.url=jdbc:postgresql://localhost:5432/stockmanager_test
spring.datasource.username=test_user
spring.datasource.password=test_password
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=test_secret_key_for_ci_cd_only
jwt.expiration=86400000

# CORS
allowed.origins=http://localhost:4200

# Logs
logging.level.org.springframework.security=WARN
logging.level.com.stockmanager=INFO